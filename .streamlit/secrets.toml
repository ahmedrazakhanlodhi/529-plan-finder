# TEMPLATE — do not commit real credentials.
# Fill in ONE of the options below, then add to Streamlit Community Cloud Secrets.

# Option A: SMTP (Gmail app password, Mailgun, Postmark, etc.)
# SMTP_HOST = "smtp.gmail.com"
# SMTP_PORT = 587
# SMTP_USER = "noreply@your-domain.com"
# SMTP_PASSWORD = "your-app-password"
# SMTP_FROM = "The 529 Network <noreply@529network.org>"

# Option B: SendGrid
# SENDGRID_API_KEY = "SG.xxxxxxxxxxxxxxxxxxxxxxxx"
